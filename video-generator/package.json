{
  "name": "videoflow",
  "version": "2.0.0",
  "description": "VideoFlow: estudio de video IA con Higgsfield en Cloudflare Workers",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply videoflow --remote",
    "catalog": "node scripts/build-catalog.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0"
  },
  "devDependencies": {
    "wrangler": "^4.140.0"
  }
}
