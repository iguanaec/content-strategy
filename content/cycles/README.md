# Ciclos de contenido

Cada corrida de `/content-cycle` crea aquí una carpeta `<cycle-id>/` (ver estructura completa en `CLAUDE.md`). No hay ningún ciclo corrido todavía — esta carpeta existe solo para que el orquestador tenga dónde escribir.
